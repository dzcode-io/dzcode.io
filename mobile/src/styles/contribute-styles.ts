import { MEDIUM_MARGIN_SIZE } from "../utils/constants";
import { StyleSheet } from "react-native";

// export contribute styles
export const contributeStyles = StyleSheet.create({
  dzcodeLoading: {
    position: "absolute",
  },
  listView: {
    marginVertical: MEDIUM_MARGIN_SIZE,
  },
});
