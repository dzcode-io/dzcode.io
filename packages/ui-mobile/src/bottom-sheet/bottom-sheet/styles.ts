import { StyleSheet } from "react-native";

export const bottomSheetStyles = StyleSheet.create({
  container: {
    borderColor: "#aaa3",
    borderWidth: 2,
    borderRadius: 16,

    // Android
    elevation: 8,
  },
});
